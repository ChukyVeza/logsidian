
```dataview
//Definimos que es una búsqueda por tareas
task
//De las notas en la carpeta diario
FROM "Registro/Diario"
where !completed and contains(text, "2024")
sort file.name
group by file.link 
```

```dataview
//Definimos que es una búsqueda por tareas
task
//De las notas en la carpeta diario
FROM "Registro/Diario"
where !completed and due >= date(2024-01-01) and due < date(2025-01-01)
sort file.name
group by file.link 
```
